/**
 * TXI HTML Style Guide Exports
 *
 * Provides HTMLHint configuration for HTML linting
 */

const htmlhintConfig = require('./htmlhint.config.js');

module.exports = {
  htmlhint: htmlhintConfig,
};
