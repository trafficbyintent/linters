# NPM Configuration for @trafficbyintent scope
# This ensures the package is published to/installed from the correct registry

# For private npm registry (if using npm Enterprise)
# @trafficbyintent:registry=https://npm.your-company.com/

# For GitHub Packages
@trafficbyintent:registry=https://npm.pkg.github.com

# Authentication token should be set via environment variable:
# NPM_TOKEN or GITHUB_TOKEN
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}